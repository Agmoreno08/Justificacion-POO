package com.agma.justificationpoo.actions;

import org.openxava.actions.*;

public class GoToStudentAction extends ViewBaseAction {

    @Override
    public void execute() throws Exception {
        getView().setModelName("Student"); // Configura la vista para redirigir a Student
        showNewView(); // Muestra la nueva vista
    }
}
